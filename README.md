# Ziying Quant Dashboard Pages

This public repository hosts the static Strategy Lab dashboard for the private `jiaweioss/ziying-quant-tool` research repository.

Live site:

```text
https://jiaweioss.github.io/ziying-quant-dashboard-pages/
```

The site is a static export of:

```text
a_share_quant/dashboard
```

It reads:

```text
data/strategy_lab_results.json
```

The private research repository remains private; this repository contains only the browser dashboard, compact backtest JSON, and static assets required for GitHub Pages.
